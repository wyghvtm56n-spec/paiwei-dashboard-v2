{
  "name": "paiwei-dashboard-v2",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.js && node --check src/meta.js && node --check src/line.js && node --check src/render.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
